#include <stdio.h>
#include <string.h>
char reverse_sentence(char input[100]);
int main(){

    //take the input from user
    char sentence[100];
    printf("Enter a sentence: ");
    fflush(stdin); fflush(stdout);
    gets(sentence);

    //puts(sentence);


    return 0;
}

char reverse_sentence(char input[100]){
    if 
}