#include<stdio.h>
void prime_numbers(int num1, int num2)
int main()
{
    int in1,in2;
    printf("Enter two numbers (intervals): ");
    fflush(stdin);fflush(stdout);
    scanf("%d %d", &in1, &in2);
    return 0;
}
void prime_numbers(int num1, int num2)
{

}