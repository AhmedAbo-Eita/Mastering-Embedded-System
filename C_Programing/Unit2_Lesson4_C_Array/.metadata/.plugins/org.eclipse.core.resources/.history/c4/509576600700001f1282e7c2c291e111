/*
 * main.c
 *
 *  Created on: Apr 21, 2024
 *      Author: es-ahmedabdellatif20
 */

#include <stdio.h>

int size = 0; // the size of array (number of user inputs)
float a[size] = 0; // matrix contains user inputs

printf("Enter the numbers of data");
fflush(stdout); fflush(stdin);
scanf("%d", &size);


